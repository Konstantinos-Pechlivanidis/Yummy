/**
 * Helper script to update all routes with asyncHandler
 * This is a reference - routes are manually updated for precision
 */

// All routes should follow this pattern:
// const asyncHandler = require("../../../middleware/asyncHandler");
// router.METHOD("/path", asyncHandler((req, res) => controller(req, res, pool)));

